[tool.poetry]
name = "telescope"
version = "0.1.0"
description = ""
authors = ["Junior Gantin <nioperas06@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
Django = "^5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
